"use strict";function showVideo(videoUrl,title){let modalTitle;document.getElementById("videoModalLabel").textContent=title;let videoContainer=document.getElementById("videoContainer");videoContainer.innerHTML="";let videoHtml=`<video id="modalVideo" width="100%" controls>\n                        <source src="${videoUrl}" type="video/mp4">\n                        Your browser does not support the video tag.\n                     </video>`,videoModal,modalElement;videoContainer.innerHTML=videoHtml,new bootstrap.Modal(document.getElementById("videoModal")).show(),document.getElementById("videoModal").addEventListener("hidden.bs.modal",(function(){let videoElement=document.getElementById("modalVideo");videoElement&&(videoElement.pause(),videoElement.currentTime=0)}))}